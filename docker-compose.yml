services:
  mysql:
    image: mysql:5
    environment:
      MYSQL_ROOT_PASSWORD: 'root'
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql

  teamcal:
    build: .
    ports:
      - "8080:80"

volumes:
  db-data:
